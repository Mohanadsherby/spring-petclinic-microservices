apiVersion: apps/v1
kind: Deployment
metadata:
  name: vets-service
  labels:
    app: vets-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: vets-service
  template:
    metadata:
      labels:
        app: vets-service
    spec:
      imagePullSecrets:
        - name: {{ .Values.imagePullSecret }}
      containers:
        - name: vets-service
          image: {{ .Values.imageRegistry }}/{{ .Values.vetsService.image }}:{{ .Values.vetsService.tag }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          envFrom:
            - configMapRef:
                name: petclinic-config
          ports:
            - containerPort: {{ .Values.vetsService.port }}
          startupProbe:
            httpGet:
              path: {{ .Values.vetsService.healthPath }}
              port: {{ .Values.vetsService.port }}
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: {{ .Values.vetsService.healthPath }}
              port: {{ .Values.vetsService.port }}
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: {{ .Values.vetsService.healthPath }}
              port: {{ .Values.vetsService.port }}
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: vets-service
  labels:
    app: vets-service
spec:
  type: ClusterIP
  selector:
    app: vets-service
  ports:
    - port: {{ .Values.vetsService.port }}
      targetPort: {{ .Values.vetsService.port }}
