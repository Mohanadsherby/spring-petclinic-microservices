# ArgoCD Application: watches git and deploys the visits-service release.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: visits-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Mohanadsherby/spring-petclinic-microservices.git
    targetRevision: main
    path: devops/helm/petclinic
    helm:
      valueFiles:
        - values/visits-service.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
